import {
  useIsMultiselection,
  useIsSelected,
  useIsSelectionEmpty,
  useIsSingleSelection,
  useSelectionSize,
} from "./queries";
import {
  useClearSelection,
  useSelectionEvent,
  getSelection,
  useSelection,
} from "./store";

export {
  useIsSelected,
  useIsSelectionEmpty,
  useIsMultiselection,
  useIsSingleSelection,
  useSelectionSize,
  useSelectionEvent,
  useClearSelection,
  getSelection,
  useSelection,
};
